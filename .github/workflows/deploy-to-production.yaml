name: Deploy-To-Production
on:
  push:
    branches:
      - master
jobs:
  Deploy-To-Production:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
      - run: npm install
      - run: npm run build
